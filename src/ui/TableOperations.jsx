function TableOperations({ children }) {
  return (
    <div className="flex flex-wrap items-center justify-end gap-[0.8rem] md:gap-[1.6rem] w-full ml-auto">
      {children}
    </div>
  );
}

export default TableOperations;
